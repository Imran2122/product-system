1) What is the difference between null and undefined?
ans:undefine means a variable is declare but not assign
null:null is value of nothing
2) What is the use of the map() function in JavaScript? How is it different from forEach()?
ans:map return new array 
forEach:not return noting
3) What is the difference between == and ===?
ans:=== check the data type and value
==only check value

4) What is the significance of async/await in fetching API data?
ans:It make api data fetching easier to read and write await promise the resolve synchronously 


5) Explain the concept of Scope in JavaScript (Global, Function, Block).
ans:global Scope:variable declared outside of the function is access from anywhere.
Function scope:variable declared inside is the function and access this variable is only access in this function
Block scope:the variable is accessible only the block scope
